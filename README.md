# Порівняння архітектур ASP.NET Core Web API

Цей репозиторій містить кілька реалізацій одного й того ж функціоналу (управління продуктами), реалізованих різними архітектурними підходами для порівняння та навчання.

> 💾 **Всі проекти використовують Entity Framework Core з SQLite для демонстрації реальної роботи з базою даних.** Докладніше в розділі [Entity Framework Core Integration](#-entity-framework-core-integration).

## 📁 Структура проектів

### 1. [3-Layer Architecture](./3-Layer-Architecture/) - Трирівнева архітектура
Найпростіша та найпоширеніша архітектура для невеликих та середніх проектів.

### 2. [Clean Architecture](./Clean-Architecture/) - Чиста архітектура
Архітектура від Uncle Bob з акцентом на незалежності бізнес-логіки від зовнішніх деталей.

### 3. [Onion Architecture](./Onion-Architecture/) - Цибулинна архітектура
Архітектура з концентричними шарами та явним виділенням Domain Services.

### 4. [Hexagonal Architecture](./Hexagonal-Architecture/) - Ports & Adapters
Концентрує бізнес-логіку в ядрі і відокремлює I/O через порти та адаптери (InMemory, EF, API clients).

### 5. [Vertical Slice Architecture](./Vertical-Slice/) - Vertical Slice
Організує код по функціональних сценаріях (features) — менше глобальних залежностей, handlers/DTO поруч зі сценарієм.

---

## 🔍 Порівняльна таблиця

| Характеристика | 3-Layer | Clean | Onion | Hexagonal | Vertical Slice |
|---------------|---------|-------|-------|-----------|----------------|
| **Складність** | ⭐ Проста | ⭐⭐⭐ Середня | ⭐⭐⭐⭐ Висока | ⭐⭐⭐ Середня-висока | ⭐⭐ Середня |
| **Швидкість розробки** | ⚡⚡⚡ Швидка | ⚡⚡ Середня | ⚡ Повільна | ⚡⚡ Середня | ⚡⚡⚡ Швидка до середньої |
| **Тестованість** | ⚠️ Середня | ✅ Висока | ✅ Дуже висока | ✅ Висока (через порти) | ✅ Висока (локалізовані слайси) |
| **Залежності від БД** | ❌ Сильна | ✅ Слабка | ✅ Низька | ✅ Слабка (через порти) | ✅ Залежить від імплементації (можна слабка) |
| **Domain в центрі** | ❌ Ні | ✅ Так | ✅ Так | ✅ Так (ядро з портами) | ⚠️ Частково (по-слайсам) |
| **Кількість шарів / підходів** | 3 | 4 | 4-5 | 3-4 (ядро + адаптери) | Feature-based (вертикальні слайси) |
| **Boilerplate код** | Мінімум | Середньо | Багато | Середньо | Мінімум-середньо |
| **Підходить для DDD** | ❌ Ні | ⚠️ Частково | ✅ Так | ✅ Так | ⚠️ Частково (можна інтегрувати)

---

### ⚠️ Про складність: важлива позначка

**Рейтинги в рядку "Складність" відображають типові імплементації, а не абсолютні факти.**

Наприклад:
- **Clean** з базовою архітектурою (entity, handler, repository interface) = ⭐⭐⭐
- **Clean + CQRS + MediatR** = ⭐⭐⭐⭐ або ⭐⭐⭐⭐⭐ (може бути складніше за Onion!)
- **Onion** з явними Domain Services і чіткими шарами = ⭐⭐⭐⭐
- **Onion** без Domain Services, просто вкладені інтерфейси = ⭐⭐⭐

**Чому я оцінив базовий Onion як складніший за базовий Clean:**
1. **Clean** мінімально потребує: 1 Entity + 1 Handler + 1 Repository Interface
2. **Onion** потребує: Entity + Domain Services + Application Services + Repository Interface + явне розділення шарів
3. **Boilerplate**: Onion генерує більше бойлерплейта через кількість шарів та явне виділення Domain Services
4. **Крива навчання**: Onion важче зрозуміти новачкам через концентричні залежності

Але якщо додати CQRS + MediatR до Clean, ситуація змінюється. **Вибір архітектури слід базувати на конкретних потребах проекту, а не на цьому рейтингу.**

---

## 📊 Детальне порівняння

### Потік залежностей

#### 3-Layer Architecture
```
┌──────────────┐
│ Controllers  │ (Presentation)
└──────┬───────┘
       ↓
┌──────▼───────┐
│   Services   │ (Business Logic)
└──────┬───────┘
       ↓
┌──────▼───────┐
│ Repositories │ (Data Access)
└──────┬───────┘
       ↓
    [Database]
```
**Проблема**: БД в центрі уваги, важко змінити

---

#### Clean Architecture
```
┌───────────────────┐
│       API         │ (Presentation)
└─────────┬─────────┘
          │
┌─────────▼──────────┐
│  Infrastructure    │ (Implementations)
│  залежить від ↓    │
└────────────────────┘
          ↑
          │ (реалізує інтерфейси)
┌─────────┴───────────┐
│    Application      │ (Use Cases, Interfaces)
└─────────┬───────────┘
          │
┌─────────▼───────────┐
│      Domain         │ (Entities) - ЦЕНТР
│ не залежить ні від  │
│       чого          │
└─────────────────────┘
```
**Перевага**: Domain повністю ізольований, інверсія залежностей через інтерфейси

---

#### Onion Architecture
```
        ┌──────────────────────┐
        │         API          │ Ring 5
        └──────────┬───────────┘
                   ↓
        ┌──────────▼───────────┐
        │   Infrastructure     │ Ring 4
        └──────────┬───────────┘
                   ↓
        ┌──────────▼───────────┐
        │ Application Services │ Ring 3
        └──────────┬───────────┘
                   ↓
        ┌──────────▼───────────┐
        │   Domain Services    │ Ring 2
        └──────────┬───────────┘
                   ↓
        ┌──────────▼───────────┐
        │    Domain Model      │ Ring 1 - ЯДРО
        └──────────────────────┘
```
**Перевага**: Явне виділення Domain Services, ідеально для DDD

---

#### Hexagonal Architecture
```
[External Adapters (UI, API, DB)]
        ↕
    [Ports / Interfaces]
        ↕
    [Domain Core (Business Logic)]
```
Порти (інтерфейси) визначені поруч із ядром; адаптери реалізують порти. Це забезпечує сильну ізоляцію ядра від I/O.

#### Vertical Slice
```
[Controller] -> [Feature Handler / Use Case] -> [Feature Repository / Domain]
```
Кожен «слайс» містить повний стек для конкретного сценарію (DTO, Handler, валідація, доступ до даних). Це локалізує зміни й зменшує залежності між фичами.


## 🎯 Де знаходиться Repository Interface?

| Архітектура | Місце розташування | Чому? |
|-------------|-------------------|-------|
| **3-Layer** | Data Access Layer | Разом з реалізацією (без інверсії) або в BLL (з інверсією) |
| **Clean** | Application Layer | Інверсія залежностей — Application визначає інтерфейси |
| **Onion** | Domain Layer | Domain визначає контракти (інтерфейси в ядрі) |
| **Hexagonal** | Ports (в ядрі) | Інтерфейси (порти) визначені В Domain Core; адаптери реалізують їх |
| **Vertical Slice** | Feature / Infrastructure | Кожен slice може мати свій репозиторій або використовувати спільний |

---

## 💡 Коли що використовувати?

### Використовуйте **3-Layer**, якщо:
- ✅ Простий CRUD додаток
- ✅ Малий проект або прототип
- ✅ Обмежений час розробки
- ✅ Команда не знайома з DDD
- ✅ Немає складної бізнес-логіки

**Приклади**: Внутрішні інструменти, admin панелі, прості API

---

### Використовуйте **Clean Architecture**, якщо:
- ✅ Середній або великий проект
- ✅ Потрібна висока тестованість
- ✅ Бізнес-логіка може змінюватися
- ✅ Хочете незалежність від frameworks
- ✅ Проект буде підтримуватися довго

**Приклади**: E-commerce, CRM системи, SaaS додатки

---

### Використовуйте **Onion Architecture**, якщо:
- ✅ Дуже складна бізнес-логіка
- ✅ Команда практикує DDD
- ✅ Потрібно чітке розділення Domain Services
- ✅ Enterprise проект з багатьма розробниками
- ✅ Domain є ключовою цінністю бізнесу

**Приклади**: Банківські системи, ERP, складні фінансові додатки

---

### Використовуйте **Hexagonal Architecture**, якщо:
- ✅ Потрібна сильна ізоляція Domain від I/O
- ✅ Хочете легко підміняти адаптери (DB, зовнішні API)
- ✅ Потребуєте чіткої абстракції портів/адаптерів

**Приклади**: Системи з різними зовнішніми інтеграціями, де важлива замінність адаптерів.

---

### Використовуйте **Vertical Slice Architecture**, якщо:
- ✅ Потрібна висока швидкість додавання нових фіч
- ✅ Хочете мінімізувати глобальні залежності між фічами
- ✅ Надаєте перевагу організації коду по сценаріях (use-cases)

**Приклади**: Продукти з частими feature-реваксами, MVP, швидке прототипування великих функціоналів.

---

## 🔑 Ключові концепції

### 1. Dependency Rule (правило залежностей)

Це **ядро Clean та Onion** архітектур, сформульоване Robert C. Martin (Uncle Bob).

**Правило**: залежності у коді мають указувати **ВСЕРЕДИНУ** (до центру, де бізнес-логіка), **НІКОЛИ** зовні.

```
┌────────────────────────────────────────────┐
│  Outer Rings (Frameworks, Databases, UI)  │
│       ↓ можна залежить від усього          │
│  ┌──────────────────────────────────────┐  │
│  │  Middle (Controllers, Gateways)      │  │
│  │       ↓ залежить                      │  │
│  │  ┌──────────────────────────────────┐│  │
│  │  │  Core (Domain, Use Cases)        ││  │
│  │  │  ↑ залежить ні від чого           ││  │
│  │  └──────────────────────────────────┘│  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

#### Порушення правила (залежність від деталей)

```csharp
// ❌ BAD: Business Logic залежить від конкретної реалізації
public class ProductService
{
    private readonly SqlProductRepository _repo; // конкретна реалізація!
    // Service прив'язаний до SQL - важко тестувати, важко змінити БД
}
```

#### Дотримання правила (залежність від абстракції)

```csharp
// ✅ GOOD: Business Logic залежить від абстракції
public class CreateProductUseCase
{
    private readonly IProductRepository _repo; // інтерфейс!
    // UseCase не знає про SQL/Mongo - легко тестувати, легко змінити
}

// Інтерфейс визначає внутрішній шар:
// - Clean: Application Layer визначає контракт
// - Onion: Domain Layer визначає контракт
// - Infrastructure реалізує інтерфейс (залежність всередину!)
```

**Ключовий момент**: це не просто "додати інтерфейс", а структурно організувати каркас так, щоб залежності йшли в один бік.

---

### 2. Domain Model: Rich vs Anemic

**Два підходи до розміщення бізнес-логіки:**

#### Rich Domain Model (DDD, Onion)
```csharp
// ✅ Логіка В Entity
public class Product
{
    private decimal _price;
    
    public void IncreasePrice(decimal percentage)
    {
        if (percentage <= 0) throw new InvalidOperationException();
        _price *= (1 + percentage / 100);
    }
}

public class UpdateProductUseCase
{
    public void Execute(int id, decimal percentage)
    {
        var product = _repo.GetById(id);
        product.IncreasePrice(percentage); // Entity знає правила
        _repo.Save(product);
    }
}
```
**Плюси**: Правила захищені, не дублюються, легко тестувати  
**Мінуси**: Складніше з ORM, більше коду в Entity

#### Anemic Domain Model (Clean, практика)
```csharp
// ✅ Логіка В Use Case
public class Product
{
    public decimal Price { get; set; } // Просто дані
}

public class UpdateProductUseCase
{
    public void Execute(int id, decimal percentage)
    {
        if (percentage <= 0) throw new InvalidOperationException();
        var product = _repo.GetById(id);
        product.Price *= (1 + percentage / 100); // Логіка тут
        _repo.Save(product);
    }
}
```
**Плюси**: Простіше з ORM, швидше писати CRUD  
**Мінуси**: Логіка може дублюватися, Entity = DTO

**Вибір**: Rich для складного Domain, Anemic для CRUD-heavy додатків

---

### 3. Repository Pattern: критична позиція в архітектурі

**Repository** — це інтерфейс доступу до даних. Його розташування сигналізує архітектуру:

| Архітектура | Де Repository Interface | Логіка | Що це говорить |
|------------|------------------------|--------|-----------------|
| **3-Layer** | Data Access Layer | Repository разом з реалізацією (вона ж конкретна) | Залежність йде ЗОВНІ (до DB) — порушення |
| **Clean** | Application Layer | Application визначає контракт, Infrastructure реалізує | Залежність йде ВСЕРЕДИНУ (Application) |
| **Onion** | Domain Layer | Domain визначає потребу, Infrastructure реалізує | Залежність йде ВСЕРЕДИНУ (Domain) |
| **Hexagonal** | Ports (ядро) | Port визначен у ядрі, Adapter реалізує зовні | Порти — це контракт між ядром і адаптерами |
| **Vertical Slice** | Feature / Domain | Інтерфейс у слайсі або спільному Kernel | Залежить від організації (може бути як Clean, так і Onion) |

**Clean Architecture**:
```csharp
// Application/Interfaces/ (контракт тут)
public interface IProductRepository { Task<Product> GetById(int id); }

// Infrastructure/ (реалізація тут)
public class EFCoreProductRepository : IProductRepository { ... }

// Application/UseCases/
public class CreateProductUseCase { private readonly IProductRepository _repo; }
```

**Onion Architecture**:
```csharp
// Domain/Interfaces/ (контракт в ядрі)
public interface IProductRepository { ... }

// Application/Services/
public class ProductApplicationService { private readonly IProductRepository _repo; }

// Infrastructure/Repositories/ (реалізація зовні)
public class ProductRepository : IProductRepository { ... }
```

---

### 4. DTO vs Domain Entity

**Різні об'єкти для різних цілей:**

```csharp
// Domain Entity (бізнес-логіка)
public class Product
{
    public int Id { get; private set; }
    public decimal Price { get; private set; }
    public void SetPrice(decimal price) { /* валідація */ }
}

// Request DTO (API контракт)
public class CreateProductRequest
{
    [Required] public string Name { get; set; }
    [Range(0.01, 999999)] public decimal Price { get; set; }
}

// Response DTO (API відповідь)
public class ProductResponse
{
    public int Id { get; set; }
    public string Name { get; set; }
    public string FormattedPrice { get; set; } // "$25.00"
}

// Use Case маппить між ними
public class CreateProductUseCase
{
    public ProductResponse Execute(CreateProductRequest request)
    {
        var entity = new Product(request.Name, request.Price); // DTO → Entity
        var saved = _repo.Save(entity);
        return new ProductResponse { /* Entity → DTO */ };
    }
}
```

**Навіщо?**  
✅ Entity не знає про HTTP/JSON  
✅ Можна мати різні DTO (Admin/User views)  
✅ API змінюється незалежно від Domain  

**Помилка**: передавати Entity прямо в Controller

---

### 5. Use Cases (Clean) vs Application Services (Onion)

Це **різні парадигми**, не синоніми!

#### Clean Architecture підхід: Use Cases

```csharp
// ✅ Один Use Case = одна операція (CQRS spirit)
// Features/Products/Application/UseCases/CreateProduct/

public class CreateProductCommand
{
    public string Name { get; set; }
    public decimal Price { get; set; }
}

public class CreateProductResponse
{
    public int Id { get; set; }
    public string Name { get; set; }
}

public class CreateProductUseCase
{
    private readonly IProductRepository _repo;
    
    public CreateProductResponse Execute(CreateProductCommand cmd)
    {
        var product = new Product(cmd.Name, cmd.Price);
        var saved = _repo.Save(product);
        return new CreateProductResponse { Id = saved.Id, Name = saved.Name };
    }
}

// Features/Products/Application/UseCases/GetProductById/

public class GetProductByIdQuery
{
    public int ProductId { get; set; }
}

public class GetProductByIdUseCase
{
    private readonly IProductRepository _repo;
    
    public ProductResponse Execute(GetProductByIdQuery query)
    {
        var product = _repo.GetById(query.ProductId);
        return new ProductResponse { /* ... */ };
    }
}
```

**У .NET часто реалізується через MediatR:**
```csharp
// MediatR обробляє routing
public class CreateProductHandler : IRequestHandler<CreateProductCommand, CreateProductResponse>
{
    public Task<CreateProductResponse> Handle(CreateProductCommand request, CancellationToken cancellationToken)
    { ... }
}
```

**Переваги Use Cases:**
- ✅ Single Responsibility (один сценарій — один клас)
- ✅ Явні Input/Output контракти
- ✅ Легше тестувати
- ✅ Явні залежності

---

#### Onion Architecture підхід: Application Services

```csharp
// ✅ Один Service = операції для однієї Entity/Aggregate
// Application/Services/

public class ProductApplicationService
{
    private readonly IProductRepository _repo;
    private readonly IProductDomainService _domainService; // Domain Service!
    
    public int Create(string name, decimal price)
    {
        var product = new Product(name, price);
        return _repo.Save(product).Id;
    }
    
    public void Update(int id, string name, decimal newPrice)
    {
        var product = _repo.GetById(id);
        
        // Domain Service може містити складну логіку
        _domainService.ValidateAndUpdate(product, newPrice);
        
        _repo.Save(product);
    }
    
    public void Delete(int id) => _repo.Delete(id);
    
    public Product GetById(int id) => _repo.GetById(id);
}
```

**Переваги Application Service:**
- ✅ Близко до традиційного Service Layer, знайоме більшості
- ✅ Методи згруповані по темі (усі операції над Product тут)
- ✅ Менше boilerplate при простих операціях

**Недоліки:**
- ❌ Менш explicit про вхід/вихід (можна забути параметр)
- ❌ Методи можуть накопичуватися (God Service)

---

**Важливо**: Clean використовує **Use Cases**, Onion використовує **Application Services**. Це різне мислення!

---

### 6. Hexagonal Architecture: Ports & Adapters

Інша парадигма. Замість "шарів" — **ядро + адаптери**.

```
┌─────────────────────────────┐
│  PORTS (Interfaces)         │ у ядрі
│  у Domain Layer             │
└────────┬────────────────────┘
         │ реалізує
┌────────▼────────────────────┐
│  ADAPTERS (Implementations) │ зовні
│  Легко змінювати            │
│  - EFCoreAdapter            │
│  - MongoDbAdapter           │
│  - InMemoryAdapter (тести)  │
└─────────────────────────────┘
```

**Port**:
```csharp
// Domain/Ports/
public interface IProductRepository // це PORT
{
    Task<Product> GetById(int id);
    Task<Product> Save(Product product);
}
```

**Adapters**:
```csharp
// Adapters/

public class EFCoreProductAdapter : IProductRepository // Adapter для EF
{
    public Task<Product> GetById(int id) { ... }
    public Task<Product> Save(Product product) { ... }
}

public class MongoDbProductAdapter : IProductRepository // Adapter для MongoDB
{
    public Task<Product> GetById(int id) { ... }
    public Task<Product> Save(Product product) { ... }
}

public class InMemoryProductAdapter : IProductRepository // Adapter для тестів
{
    private Dictionary<int, Product> _data = new();
    public Task<Product> GetById(int id) { ... }
    public Task<Product> Save(Product product) { ... }
}
```

**Чим це відрізняється від Clean?**

| Цей аспект | Clean | Hexagonal |
|-----------|-------|-----------|
| Інтерфейс де? | Application Layer | Domain Layer (Port) |
| Фокус | Архітектура слоїв | Замінність адаптерів |
| Тестування | Mock інтерфейс | Swap Adapter (InMemory) |
| Логіка | Use Cases | Domain + Ports |

**Переваги Hexagonal:**
- ✅ **Адаптери легко міняти**: тести → InMemory, продакшн → EF, завтра → MongoDB
- ✅ **Domain не знає про реалізацію** (максимальна ізоляція)
- ✅ добре для систем з множинними зовнішніми інтеграціями

---

### 7. Vertical Slice Architecture: інший світогляд

На відміну від **layer-based** (шари), це **feature-based** організація.

```
ШАРИ (Layer-based)          СЛАЙСИ (Feature-based)
src/                        src/Features/
├─ Controllers/              ├─ Products/              ← Група по ентіті
├─ Services/                 │  ├─ CreateProduct/
├─ Repositories/             │  │  ├─ Handler.cs
└─ Models/                   │  │  └─ Validator.cs
                            │  ├─ GetProducts/
                            │  │  └─ Handler.cs
                            │  └─ UpdateProduct/
                            │     └─ Handler.cs
                            └─ Infrastructure/        ← Спільна інфраструктура
                               ├─ AppDbContext.cs
                               └─ IProductRepository.cs
```

**Принцип**: Кожна фіча = повний стек (Handler → Domain/Data)

**Плюси**: Локальні зміни, швидко додавати фічі, менше конфліктів  
**Мінуси**: Потрібна дисципліна, щоб уникнути дублювання спільної логіки

---

### 8. Dependency Injection - збирання всього разом

```csharp
// Program.cs (Composition Root)

// Domain (не залежить ні від чого)
services.AddScoped<IProductDomainService, ProductDomainService>();

// Application (залежить від Domain + інтерфейсів)
services.AddScoped<CreateProductUseCase>();

// Infrastructure (реалізує інтерфейси)
services.AddScoped<IProductRepository, EFCoreProductRepository>();
services.AddDbContext<ApplicationDbContext>();
```

**DI забезпечує Dependency Rule**: Application/Domain не знають про реалізацію

---

## ⚠️ Типові помилки розробників

### 1. Плутати інверсію залежностей з просто додаванням інтерфейсів
```csharp
// ❌ Це не архітектура, це просто "більше коду"
public interface IProductRepository { }
public class ProductRepository : IProductRepository { }
public class ProductService { private readonly IProductRepository _repo; } // але Service у середині, Repository зовні
```

### 2. Вважати, що Clean = обов'язково Rich Domain Model
Clean часто використовує **Anemic Domain Model** з логікою у **Use Cases**. Це не менш Clean!

### 3. Комбінувати концепції випадково
- "Vertical Slice Onion" = плутанина (або слої, або фічи)
- "Hexagonal Clean" = можливо, але це імплементація, не архітектура
- "CQRS 3-Layer" = додає складність без користі

### 4. Ставити Entity = Request Body
```csharp
// ❌ Змішана архітектура
[HttpPost]
public void Create(Product product) // Product = Entity = DTO? Неясно!
```

### 5. Забувати про DTO
```csharp
// ❌ Лікує JSON напряму в Entity
public class Product
{
    [JsonPropertyName("id")]
    public int Id { get; set; }
}
// Тепер Entity знає про JSON. Це шум.
```

### 6. Надто рано (або надто пізно) вводити архітектуру
- **CRUD на три таблиці?** Почніть з 3-Layer. DI, Use Cases потім.
- **Складна бізнес-логіка?** Clean чи Onion з самого початку.

### 7. Забувати про контекст проекту
- Стартап = швидкість (3-Layer, можливо Vertical Slice)
- Enterprise = якість (Clean, Onion)
- Мікросервіс = рівновага (Clean + Vertical)

---

## 📈 Еволюція архітектури

### Хронологічна шкала

```
   2000s          2005              2008              2012              2020s
     |             |                 |                 |                 |
     └─ 3-Layer    │                 │                 │                 │
        (CRUD)     ├─ Hexagonal      │                 │                 │
                   │  (Alistair      │                 │                 │
                   │   Cockburn)     │                 │                 │
                   │                 ├─ Onion          │                 │
                   │                 │  (Jeffrey       │                 │
                   │                 │   Palermo)      │                 │
                   │                 │                 ├─ Clean          │
                   │                 │                 │  (Uncle Bob)    │
                   │                 │                 │                 │
                   │                 │                 │                 ├─ Vertical Slice
                   │                 │                 │                 │  (Jimmy Bogard)
                   └─────────────────┴─────────────────┴─────────────────┘
                            Всі базуються на Dependency Inversion Principle
```

### Відносини між архітектурами

```
Hexagonal (2005) ────┐
                     ├──→ Dependency Inversion Principle
Onion (2008) ────────┤    (залежності всередину)
                     │
Clean (2012) ────────┘
     │
     └──→ Vertical Slice (2020s)
          (фічі замість шарів, але з тими ж принципами)

3-Layer (1990s-2000s) — окремо, без інверсії залежностей
```

### Від Onion до Clean: еволюція ідей

**Onion Architecture (2008)** — Jeffrey Palermo
- Ввів концепцію концентричних шарів
- Domain в центрі, залежності вказують всередину
- Явне виділення Domain Services

**Clean Architecture (2012)** — Robert C. Martin (Uncle Bob)
- Розвинув ідеї Onion, але з іншим фокусом
- Менше про кількість шарів, більше про залежності
- Адаптивна структура: Application може мати різні шари
- Clean базується на **тих же принципах**, що й Onion:
  - ✅ Domain в центрі
  - ✅ Залежності вказують всередину
  - ✅ Низька залежність від фреймворків
  - ✅ Висока тестованість

**Ключова різниця**: 
- **Onion** говорить "обов'язково повинно бути 4-5 явних шарів"
- **Clean** говорить "залежності повинні вказувати в один центр, структура адаптується до потреб"

**Висновок**: Clean Architecture не є прямою еволюцією Onion, але вони —  **сестринські архітектури**, засновані на одних принципах. Clean більше про філософію залежностей, Onion більше про чіткість шарів.

### Практичні наслідки для розробника

Багато проектів використовують гібридні підходи:
- **Початок**: 3-Layer (швидко й просто)
- **Зростання**: Clean + деякі ідеї з Onion (баланс)
- **Складний домен**: Onion + DDD (максимальна чистота)
- **Частові фіч**: Vertical Slice (швидке додавання)

---

## 🧪 Як запустити проекти

Запустіть потрібний проект у окремому терміналі. При запуску Swagger доступний за адресою, яку покаже вивід `dotnet run` (зазвичай https://localhost:5001 або інший вільний порт).

### 1. 3-Layer Architecture
```powershell
cd "3-Layer-Architecture"
dotnet restore
dotnet run
```

### 2. Clean Architecture
```powershell
cd "Clean-Architecture\CleanArchitecture.API"
dotnet restore
dotnet run
```

### 3. Onion Architecture
```powershell
cd "Onion-Architecture\OnionArchitecture.API"
dotnet restore
dotnet run
```

### 4. Hexagonal Architecture
```powershell
cd "Hexagonal-Architecture\Hexagonal.API"
dotnet restore
dotnet run
```

### 5. Vertical Slice
```powershell
cd "Vertical-Slice\VerticalSlice.API"
dotnet restore
dotnet run
```

Відкрийте Swagger UI для кожного запущеного сервісу за адресою, яку виведе `dotnet run` (наприклад `https://localhost:5001/swagger`).

---

## � Entity Framework Core Integration

Всі приклади використовують **Entity Framework Core** з **SQLite** для демонстрації реальної роботи з базою даних.

### Що додано:

#### 1. **DbContext**
Кожен проект містить власний DbContext для управління з'єднанням з базою даних:
- **3-Layer**: `AppDbContext` в DataAccessLayer
- **Clean Architecture**: `ApplicationDbContext` в Infrastructure
- **Hexagonal**: `ProductDbContext` в Adapters
- **Onion**: `ApplicationDbContext` в Infrastructure
- **Vertical Slice**: `AppDbContext` в Infrastructure

#### 2. **Конфігурація моделей**
Всі моделі сконфігуровано через Fluent API з:
- Обмеженнями полів (Required, MaxLength)
- Типами даних (decimal для Price)
- Seed даними для початкового заповнення

#### 3. **Connection String**
В кожному `appsettings.json` додано:
```json
"ConnectionStrings": {
  "DefaultConnection": "Data Source=products.db"
}
```

#### 4. **Автоматична міграція**
При запуску кожного проекту база даних створюється автоматично через `db.Database.EnsureCreated()`:
```csharp
using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
    db.Database.EnsureCreated();
}
```

### Альтернативні реалізації:

В деяких проектах (Hexagonal, Vertical Slice) залишено також **In-Memory** реалізації репозиторіїв (`InMemoryProductRepository`), які можна використовувати замість EF Core, просто змінивши реєстрацію в `Program.cs`:

```csharp
// Використання EF Core Repository
builder.Services.AddScoped<IProductRepository, EfCoreProductRepository>();

// АБО

// Використання In-Memory Repository (для тестування або прототипів)
builder.Services.AddSingleton<IProductRepository, InMemoryProductRepository>();
```

Це демонструє **гнучкість архітектури** - можливість легко змінювати реалізацію без зміни бізнес-логіки.

### База даних:

Після першого запуску проекту в його директорії з'являється файл `products.db` - це SQLite база даних. Її можна переглянути будь-яким SQLite клієнтом (наприклад, DB Browser for SQLite або розширенням для VS Code).

---

## 📚 Корисні ресурси

### Книги
- **"Clean Architecture"** - Robert C. Martin
- **"Domain-Driven Design"** - Eric Evans
- **"Implementing Domain-Driven Design"** - Vaughn Vernon

### Статті
- [The Onion Architecture](https://jeffreypalermo.com/2008/07/the-onion-architecture-part-1/) - Jeffrey Palermo
- [Clean Architecture](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html) - Uncle Bob

---

## ✅ Висновки

| Якщо вам потрібно... | Використовуйте |
|---------------------|----------------|
| Швидко зробити MVP | 3-Layer |
| Баланс між простотою та якістю | Clean |
| Максимальна чистота коду | Onion |
| Простий CRUD | 3-Layer |
| Довгостроковий проект | Clean або Onion |
| DDD підхід | Onion |

**Золоте правило**: Почніть з найпростішого рішення, що вирішує вашу задачу. Ускладнюйте тільки коли це дійсно потрібно.

---

## 🤝 Поради

1. **Не over-engineer**: Не використовуйте Onion для TODO списку
2. **Думайте про майбутнє**: Якщо проект буде рости, краще одразу Clean
3. **Команда важлива**: Складні архітектури потребують досвідченої команди
4. **Тестуйте**: Всі три архітектури можна тестувати, але Clean/Onion - краще
5. **Документуйте**: Особливо важливо для Clean/Onion

Удачі у виборі правильної архітектури! 🚀
